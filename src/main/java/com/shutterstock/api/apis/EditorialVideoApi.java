/*
 * ShutterstockApiExplorerLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.shutterstock.api.apis;

import com.shutterstock.api.ApiHelper;
import com.shutterstock.api.DateTimeHelper;
import com.shutterstock.api.Server;
import com.shutterstock.api.exceptions.ApiException;
import com.shutterstock.api.http.request.HttpMethod;
import com.shutterstock.api.http.response.ApiResponse;
import com.shutterstock.api.models.DownloadAvailability;
import com.shutterstock.api.models.DownloadHistoryDataList;
import com.shutterstock.api.models.EditorialVideoCategoryResults;
import com.shutterstock.api.models.EditorialVideoContent;
import com.shutterstock.api.models.EditorialVideoResults;
import com.shutterstock.api.models.EditorialVideoSearchResults;
import com.shutterstock.api.models.LicenseEditorialContentResults;
import com.shutterstock.api.models.LicenseEditorialVideoContentRequest;
import com.shutterstock.api.models.Resolution;
import com.shutterstock.api.models.Sort17;
import com.shutterstock.api.models.Sort5;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class EditorialVideoApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public EditorialVideoApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * This endpoint searches for editorial videos. If you specify more than one search parameter,
     * the API uses an AND condition. For example, if you set the `category` parameter to
     * "Alone,Performing" and also specify a `query` parameter, the results include only videos that
     * match the query and are in both the Alone and Performing categories. You can also filter
     * search terms out in the `query` parameter by prefixing the term with NOT.
     * @param  country  Required parameter: Show only editorial video content that is available for
     *         distribution in a certain country
     * @param  query  Optional parameter: One or more search terms separated by spaces
     * @param  sort  Optional parameter: Sort by
     * @param  category  Optional parameter: Show editorial content with each of the specified
     *         editorial categories; specify category names in a comma-separated list
     * @param  supplierCode  Optional parameter: Show only editorial video content from certain
     *         suppliers
     * @param  dateStart  Optional parameter: Show only editorial video content generated on or
     *         after a specific date
     * @param  dateEnd  Optional parameter: Show only editorial video content generated on or before
     *         a specific date
     * @param  resolution  Optional parameter: Show only editorial video content with specific
     *         resolution
     * @param  fps  Optional parameter: Show only editorial video content generated with specific
     *         frames per second
     * @param  perPage  Optional parameter: Number of results per page
     * @param  cursor  Optional parameter: The cursor of the page with which to start fetching
     *         results; this cursor is returned from previous requests
     * @return    Returns the EditorialVideoSearchResults wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<EditorialVideoSearchResults> searchEditorialVideos(
            final String country,
            final String query,
            final Sort17 sort,
            final String category,
            final List<String> supplierCode,
            final LocalDate dateStart,
            final LocalDate dateEnd,
            final Resolution resolution,
            final Double fps,
            final Integer perPage,
            final String cursor) throws ApiException, IOException {
        return prepareSearchEditorialVideosRequest(country, query, sort, category, supplierCode,
                dateStart, dateEnd, resolution, fps, perPage, cursor).execute();
    }

    /**
     * This endpoint searches for editorial videos. If you specify more than one search parameter,
     * the API uses an AND condition. For example, if you set the `category` parameter to
     * "Alone,Performing" and also specify a `query` parameter, the results include only videos that
     * match the query and are in both the Alone and Performing categories. You can also filter
     * search terms out in the `query` parameter by prefixing the term with NOT.
     * @param  country  Required parameter: Show only editorial video content that is available for
     *         distribution in a certain country
     * @param  query  Optional parameter: One or more search terms separated by spaces
     * @param  sort  Optional parameter: Sort by
     * @param  category  Optional parameter: Show editorial content with each of the specified
     *         editorial categories; specify category names in a comma-separated list
     * @param  supplierCode  Optional parameter: Show only editorial video content from certain
     *         suppliers
     * @param  dateStart  Optional parameter: Show only editorial video content generated on or
     *         after a specific date
     * @param  dateEnd  Optional parameter: Show only editorial video content generated on or before
     *         a specific date
     * @param  resolution  Optional parameter: Show only editorial video content with specific
     *         resolution
     * @param  fps  Optional parameter: Show only editorial video content generated with specific
     *         frames per second
     * @param  perPage  Optional parameter: Number of results per page
     * @param  cursor  Optional parameter: The cursor of the page with which to start fetching
     *         results; this cursor is returned from previous requests
     * @return    Returns the EditorialVideoSearchResults wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<EditorialVideoSearchResults>> searchEditorialVideosAsync(
            final String country,
            final String query,
            final Sort17 sort,
            final String category,
            final List<String> supplierCode,
            final LocalDate dateStart,
            final LocalDate dateEnd,
            final Resolution resolution,
            final Double fps,
            final Integer perPage,
            final String cursor) {
        try {
            return prepareSearchEditorialVideosRequest(country, query, sort, category, supplierCode,
            dateStart, dateEnd, resolution, fps, perPage, cursor).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for searchEditorialVideos.
     */
    private ApiCall<ApiResponse<EditorialVideoSearchResults>, ApiException> prepareSearchEditorialVideosRequest(
            final String country,
            final String query,
            final Sort17 sort,
            final String category,
            final List<String> supplierCode,
            final LocalDate dateStart,
            final LocalDate dateEnd,
            final Resolution resolution,
            final Double fps,
            final Integer perPage,
            final String cursor) {
        return new ApiCall.Builder<ApiResponse<EditorialVideoSearchResults>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v2/editorial/videos/search")
                        .queryParam(param -> param.key("country")
                                .value(country))
                        .queryParam(param -> param.key("query")
                                .value(query).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value((sort != null) ? sort.value() : "relevant").isRequired(false))
                        .queryParam(param -> param.key("category")
                                .value(category).isRequired(false))
                        .queryParam(param -> param.key("supplier_code")
                                .value(supplierCode).isRequired(false))
                        .queryParam(param -> param.key("date_start")
                                .value(DateTimeHelper.toSimpleDate(dateStart)).isRequired(false))
                        .queryParam(param -> param.key("date_end")
                                .value(DateTimeHelper.toSimpleDate(dateEnd)).isRequired(false))
                        .queryParam(param -> param.key("resolution")
                                .value((resolution != null) ? resolution.value() : null).isRequired(false))
                        .queryParam(param -> param.key("fps")
                                .value(fps).isRequired(false))
                        .queryParam(param -> param.key("per_page")
                                .value((perPage != null) ? perPage : 20).isRequired(false))
                        .queryParam(param -> param.key("cursor")
                                .value(cursor).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("basic")
                                        .add("customer_accessCode")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, EditorialVideoSearchResults.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Bad Request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("406",
                                 ErrorCase.setReason("Not Acceptable",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint lists the categories that editorial videos can belong to, which are separate
     * from the categories that other types of assets can belong to.
     * @return    Returns the EditorialVideoCategoryResults wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<EditorialVideoCategoryResults> listEditorialVideoCategories() throws ApiException, IOException {
        return prepareListEditorialVideoCategoriesRequest().execute();
    }

    /**
     * This endpoint lists the categories that editorial videos can belong to, which are separate
     * from the categories that other types of assets can belong to.
     * @return    Returns the EditorialVideoCategoryResults wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<EditorialVideoCategoryResults>> listEditorialVideoCategoriesAsync() {
        try {
            return prepareListEditorialVideoCategoriesRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listEditorialVideoCategories.
     */
    private ApiCall<ApiResponse<EditorialVideoCategoryResults>, ApiException> prepareListEditorialVideoCategoriesRequest() {
        return new ApiCall.Builder<ApiResponse<EditorialVideoCategoryResults>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v2/editorial/videos/categories")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("basic")
                                        .add("customer_accessCode")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, EditorialVideoCategoryResults.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Bad Request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint shows information about an editorial image, including a URL to a preview image
     * and the sizes that it is available in.
     * @param  id  Required parameter: Editorial ID
     * @param  country  Required parameter: Returns only if the content is available for
     *         distribution in a certain country
     * @param  searchId  Optional parameter: The ID of the search that is related to this request
     * @return    Returns the EditorialVideoContent wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<EditorialVideoContent> getEditorialVideo(
            final String id,
            final String country,
            final String searchId) throws ApiException, IOException {
        return prepareGetEditorialVideoRequest(id, country, searchId).execute();
    }

    /**
     * This endpoint shows information about an editorial image, including a URL to a preview image
     * and the sizes that it is available in.
     * @param  id  Required parameter: Editorial ID
     * @param  country  Required parameter: Returns only if the content is available for
     *         distribution in a certain country
     * @param  searchId  Optional parameter: The ID of the search that is related to this request
     * @return    Returns the EditorialVideoContent wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<EditorialVideoContent>> getEditorialVideoAsync(
            final String id,
            final String country,
            final String searchId) {
        try {
            return prepareGetEditorialVideoRequest(id, country, searchId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getEditorialVideo.
     */
    private ApiCall<ApiResponse<EditorialVideoContent>, ApiException> prepareGetEditorialVideoRequest(
            final String id,
            final String country,
            final String searchId) {
        return new ApiCall.Builder<ApiResponse<EditorialVideoContent>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v2/editorial/videos/{id}")
                        .queryParam(param -> param.key("country")
                                .value(country))
                        .queryParam(param -> param.key("search_id")
                                .value(searchId).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("basic")
                                        .add("customer_accessCode")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, EditorialVideoContent.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Bad Request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("406",
                                 ErrorCase.setReason("Not Acceptable",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint lists the details of editorial videos by ID list.
     * @param  id  Required parameter: ID of the editorial video to list details for
     * @param  country  Required parameter: Show only editorial video content that is available for
     *         distribution in a certain country
     * @param  searchId  Optional parameter: The ID of the search that is related to this request
     * @return    Returns the EditorialVideoResults wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<EditorialVideoResults> listEditorialVideos(
            final List<String> id,
            final String country,
            final String searchId) throws ApiException, IOException {
        return prepareListEditorialVideosRequest(id, country, searchId).execute();
    }

    /**
     * This endpoint lists the details of editorial videos by ID list.
     * @param  id  Required parameter: ID of the editorial video to list details for
     * @param  country  Required parameter: Show only editorial video content that is available for
     *         distribution in a certain country
     * @param  searchId  Optional parameter: The ID of the search that is related to this request
     * @return    Returns the EditorialVideoResults wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<EditorialVideoResults>> listEditorialVideosAsync(
            final List<String> id,
            final String country,
            final String searchId) {
        try {
            return prepareListEditorialVideosRequest(id, country, searchId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listEditorialVideos.
     */
    private ApiCall<ApiResponse<EditorialVideoResults>, ApiException> prepareListEditorialVideosRequest(
            final List<String> id,
            final String country,
            final String searchId) {
        return new ApiCall.Builder<ApiResponse<EditorialVideoResults>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v2/editorial/videos")
                        .queryParam(param -> param.key("id")
                                .value(id))
                        .queryParam(param -> param.key("country")
                                .value(country))
                        .queryParam(param -> param.key("search_id")
                                .value(searchId).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("basic")
                                        .add("customer_accessCode")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, EditorialVideoResults.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Bad Request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint lists existing editorial video licenses.
     * @param  videoId  Optional parameter: Show licenses for the specified editorial video ID
     * @param  license  Optional parameter: Show editorial videos that are available with the
     *         specified license name
     * @param  page  Optional parameter: Page number
     * @param  perPage  Optional parameter: Number of results per page
     * @param  sort  Optional parameter: Sort order
     * @param  username  Optional parameter: Filter licenses by username of licensee
     * @param  startDate  Optional parameter: Show licenses created on or after the specified date
     * @param  endDate  Optional parameter: Show licenses created before the specified date
     * @param  downloadAvailability  Optional parameter: Filter licenses by download availability
     * @param  teamHistory  Optional parameter: Set to true to see license history for all members
     *         of your team.
     * @return    Returns the DownloadHistoryDataList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DownloadHistoryDataList> getEditorialVideoLicenseList(
            final String videoId,
            final String license,
            final Integer page,
            final Integer perPage,
            final Sort5 sort,
            final String username,
            final LocalDateTime startDate,
            final LocalDateTime endDate,
            final DownloadAvailability downloadAvailability,
            final Boolean teamHistory) throws ApiException, IOException {
        return prepareGetEditorialVideoLicenseListRequest(videoId, license, page, perPage, sort,
                username, startDate, endDate, downloadAvailability, teamHistory).execute();
    }

    /**
     * This endpoint lists existing editorial video licenses.
     * @param  videoId  Optional parameter: Show licenses for the specified editorial video ID
     * @param  license  Optional parameter: Show editorial videos that are available with the
     *         specified license name
     * @param  page  Optional parameter: Page number
     * @param  perPage  Optional parameter: Number of results per page
     * @param  sort  Optional parameter: Sort order
     * @param  username  Optional parameter: Filter licenses by username of licensee
     * @param  startDate  Optional parameter: Show licenses created on or after the specified date
     * @param  endDate  Optional parameter: Show licenses created before the specified date
     * @param  downloadAvailability  Optional parameter: Filter licenses by download availability
     * @param  teamHistory  Optional parameter: Set to true to see license history for all members
     *         of your team.
     * @return    Returns the DownloadHistoryDataList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DownloadHistoryDataList>> getEditorialVideoLicenseListAsync(
            final String videoId,
            final String license,
            final Integer page,
            final Integer perPage,
            final Sort5 sort,
            final String username,
            final LocalDateTime startDate,
            final LocalDateTime endDate,
            final DownloadAvailability downloadAvailability,
            final Boolean teamHistory) {
        try {
            return prepareGetEditorialVideoLicenseListRequest(videoId, license, page, perPage, sort,
            username, startDate, endDate, downloadAvailability, teamHistory).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getEditorialVideoLicenseList.
     */
    private ApiCall<ApiResponse<DownloadHistoryDataList>, ApiException> prepareGetEditorialVideoLicenseListRequest(
            final String videoId,
            final String license,
            final Integer page,
            final Integer perPage,
            final Sort5 sort,
            final String username,
            final LocalDateTime startDate,
            final LocalDateTime endDate,
            final DownloadAvailability downloadAvailability,
            final Boolean teamHistory) {
        return new ApiCall.Builder<ApiResponse<DownloadHistoryDataList>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v2/editorial/videos/licenses")
                        .queryParam(param -> param.key("video_id")
                                .value(videoId).isRequired(false))
                        .queryParam(param -> param.key("license")
                                .value(license).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("per_page")
                                .value((perPage != null) ? perPage : 20).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value((sort != null) ? sort.value() : "newest").isRequired(false))
                        .queryParam(param -> param.key("username")
                                .value(username).isRequired(false))
                        .queryParam(param -> param.key("start_date")
                                .value(DateTimeHelper.toRfc8601DateTime(startDate)).isRequired(false))
                        .queryParam(param -> param.key("end_date")
                                .value(DateTimeHelper.toRfc8601DateTime(endDate)).isRequired(false))
                        .queryParam(param -> param.key("download_availability")
                                .value((downloadAvailability != null) ? downloadAvailability.value() : "all").isRequired(false))
                        .queryParam(param -> param.key("team_history")
                                .value((teamHistory != null) ? teamHistory : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("customer_accessCode"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DownloadHistoryDataList.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Bad Request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint gets licenses for one or more editorial videos. You must specify the country
     * and one or more editorial videos to license. The download links in the response are valid for
     * 8 hours.
     * @param  body  Required parameter: License editorial video content
     * @return    Returns the LicenseEditorialContentResults wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<LicenseEditorialContentResults> licenseEditorialVideo(
            final LicenseEditorialVideoContentRequest body) throws ApiException, IOException {
        return prepareLicenseEditorialVideoRequest(body).execute();
    }

    /**
     * This endpoint gets licenses for one or more editorial videos. You must specify the country
     * and one or more editorial videos to license. The download links in the response are valid for
     * 8 hours.
     * @param  body  Required parameter: License editorial video content
     * @return    Returns the LicenseEditorialContentResults wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<LicenseEditorialContentResults>> licenseEditorialVideoAsync(
            final LicenseEditorialVideoContentRequest body) {
        try {
            return prepareLicenseEditorialVideoRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for licenseEditorialVideo.
     */
    private ApiCall<ApiResponse<LicenseEditorialContentResults>, ApiException> prepareLicenseEditorialVideoRequest(
            final LicenseEditorialVideoContentRequest body) {
        return new ApiCall.Builder<ApiResponse<LicenseEditorialContentResults>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/v2/editorial/videos/licenses")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("customer_accessCode"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, LicenseEditorialContentResults.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Bad Request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("Unauthorized",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("Forbidden",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}