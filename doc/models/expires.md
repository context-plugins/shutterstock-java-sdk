
# Expires

Whether or not the token expires, expiring tokens come with a refresh_token to renew the access_token

## Enumeration

`Expires`

## Fields

| Name |
|  --- |
| `ENUM_TRUE` |
| `ENUM_FALSE` |

## Example

```java
import com.shutterstock.api.models.Expires;

Expires expires = Expires.ENUM_TRUE;
```

